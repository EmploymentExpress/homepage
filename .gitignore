__pycache__/
*.py[cod]
.DS_Store
.venv/
venv/

# Local review artifacts (sample thumbnails, user-uploaded reference images)
/uploads/

# Local read-only source diagnostics; CI uploads these as artifacts, never commits them.
.cache/source-access/
